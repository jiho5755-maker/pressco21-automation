# ========================
# 환경변수 설정 가이드
# ========================
#
# 이 파일을 복사하여 .env.local 파일을 생성하세요:
# cp .env.example .env.local
#
# ========================

# ── 데이터베이스 (로컬 개발) ──
# 로컬: SQLite 사용 (빠른 개발)
DATABASE_URL="file:./dev.db"

# 프로덕션: PostgreSQL (Vercel + Neon)
# DATABASE_URL="postgresql://user:password@host/database?sslmode=require"

# ── Auth.js 설정 ──
# JWT 서명 키 (32자 이상, openssl rand -base64 32로 생성)
AUTH_SECRET="your-random-32-character-secret-key-here"

# 관리자 계정 (시드 데이터 생성용)
AUTH_ADMIN_EMAIL="admin@company.com"
AUTH_ADMIN_PASSWORD="admin1234"

# ── 앱 설정 ──
NEXT_PUBLIC_APP_NAME="소기업 자동화 시스템"

# ── 이메일 알림 (Resend) ──
# Resend API 키 (https://resend.com/api-keys)
RESEND_API_KEY="re_xxxxxxxxxxxxxxxxxxxxxxxxxx"

# 발신자 이메일 (Resend에서 도메인 인증 필요)
# 예시: "PRESSCO21 <noreply@pressco21.com>"
RESEND_FROM_EMAIL="PRESSCO21 <admin@pressco21.com>"

# ========================
# 프로덕션 환경 (Vercel)
# ========================
#
# Vercel 대시보드에서 다음 환경변수를 설정하세요:
#
# 1. DATABASE_URL (Neon PostgreSQL)
#    - Vercel Integration에서 자동 생성됨
#
# 2. AUTH_SECRET (Production/Preview only)
#    - 로컬과 다른 키 사용
#
# 3. AUTH_ADMIN_EMAIL (All Environments)
#
# 4. AUTH_ADMIN_PASSWORD (Production/Preview only, Sensitive)
#
# 5. NEXT_PUBLIC_APP_NAME (All Environments)
#
